{
  "name": "veritas-ai-extension",
  "version": "0.1.0",
  "description": "VeritasAI Chrome extension — real-time fact-checking via a side panel",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
